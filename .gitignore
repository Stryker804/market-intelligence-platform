# dependencies
node_modules/

# secrets / local env
.env
.env.*
!.env.example

# logs & dev/test artifacts
*.log
test-results/
playwright-report/
screenshots/

# app runtime data (regenerated by the pipeline; fixtures must stay tracked)
app/data/state/
app/data/history/
app/data/raw/
app/data/opportunities/
app/data/_backup/
app/data/fx.json

# legacy research market data / local dumps (not part of app runtime surface)
data/